{
  "name": "siem-lite",
  "version": "1.0.0",
  "description": "Industry-Ready SIEM - Security Information & Event Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev",
    "setup": "cd server && npm install && cd ../client && npm install",
    "seed": "cd server && node db/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
